// Default store list — single source of truth, imported by the budget UI
// (AppSettings, BudgetDashboard) and by the backup-key registry in exportBackup.js
// (so the census can tell a user-edited store list from the default).
export const DEFAULT_STORES = ["Amazon", "Best Buy", "Bricklink", "LEGO", "Target", "Walmart"];
